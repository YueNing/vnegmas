from typing import Optional

# TODO Implement the function that to analyse the stats data return from log folder
# and return the finale data that used on frontend 
def _monitor_every_info(data: dict) -> dict:
    return data